import React from "react"

const Navigation = ({ menu }) => {
  return (
    <div>
      <h1>Navigation</h1>
    </div>
  )
}

// Navigation.propTypes = {
//   menu: PropTypes.
// }

export default Navigation
